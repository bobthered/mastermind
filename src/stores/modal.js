import { store as error } from '$components/Modal/Error';
import { store as lost } from '$components/Modal/Lost';
import { store as spinner } from '$components/Modal/Spinner';
import { store as win } from '$components/Modal/Win';

export default {
  error,
  lost,
  spinner,
  win,
}